'use strict';

// Register `phoneDetail` component, along with its associated controller and template
angular.
  module('missionTest').
  component('missionTest', {
    templateUrl: 'scripts/mission-test/mission-test.template.html',
    controller: function MissionTestController() {
        var self = this;

      }
  });
